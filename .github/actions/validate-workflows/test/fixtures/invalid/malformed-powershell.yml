name: Malformed PowerShell
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: windows-latest
    steps:
      - shell: pwsh
        run: |
          if ($true) {
            Write-Host "missing closing brace"
